Allows methods to do different things based on the object it is acting upon even though the method name and its signature might be same

compile time polymorphism
    - method overloading 
    - same method name but different parameters

runtime polymorphism
    - method overriding in inheritance
    - dynamic method dispatch -> decides on runtime which method to call 