### Maps in java
```java
Map<Character,Integer> freqMap1 = new HashMap<>();

// check for a value , put if doesn't exist or compute if exist
reqMap1.merge(a, 1, Integer::sum);

// iterate over keys or values
Character key : freqMap1.keySet()
Character key : freqMap1.values()
```